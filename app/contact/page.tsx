import { Contact } from "@/components/contact";

export const metadata = { title: "Contact — Mariem Sabri" };

export default function ContactPage() {
  return (
    <div className="pt-16">
      <Contact />
    </div>
  );
}
